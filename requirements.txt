# ── RunPod serverless runtime ─────────────────────────────────────────────────
runpod>=1.7.0

# ── AWS S3 / Cloudflare R2 / MinIO ────────────────────────────────────────────
boto3>=1.34.0

# ── TTS ───────────────────────────────────────────────────────────────────────
edge-tts>=6.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.31.0
pyyaml>=6.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
# NumPy 2.x breaks basicsr / facexlib / realesrgan — pin to 1.x
numpy>=1.24,<2.0
# Audio processing — used by audio_dataset.py inside HunyuanVideo-Avatar
librosa>=0.10.0
decord>=0.6.0

# ── HuggingFace (model downloads) ─────────────────────────────────────────────
huggingface_hub>=0.23.0

# ── Face detection (used in preprocess_avatar) ────────────────────────────────
insightface>=0.7.3
onnxruntime-gpu>=1.18.0   # required by insightface on GPU

# ── Face enhancement ──────────────────────────────────────────────────────────
# basicsr is intentionally NOT listed here — it is installed from the XPixelGroup
# git repo in the Dockerfile AFTER all other packages so that realesrgan/facexlib
# cannot overwrite it with the torchvision-incompatible PyPI version.
facexlib>=0.3.0
realesrgan>=0.3.0
