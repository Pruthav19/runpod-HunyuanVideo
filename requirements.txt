# ── RunPod serverless runtime ─────────────────────────────────────────────────
runpod>=1.7.0

# ── AWS S3 / Cloudflare R2 / MinIO ────────────────────────────────────────────
boto3>=1.34.0

# ── TTS ───────────────────────────────────────────────────────────────────────
edge-tts>=6.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.31.0
pyyaml>=6.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# ── HuggingFace (model downloads) ─────────────────────────────────────────────
huggingface_hub>=0.23.0

# ── Face enhancement (CodeFormer preferred over GFPGAN for identity stability) ─
# Install codeformer from source — pip package is outdated
# gfpgan>=1.3.8   ← replaced by CodeFormer below
# basicsr>=1.4.2 removed — torchvision 0.17+ dropped rgb_to_grayscale from
# torchvision.transforms.functional_tensor which basicsr<1.4.2 imports at startup.
# The upstream XPixelGroup repo has the fix; use it directly.
basicsr @ git+https://github.com/XPixelGroup/BasicSR.git
facexlib>=0.3.0
realesrgan>=0.3.0
